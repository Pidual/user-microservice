server:
  port: 8091

spring:
  datasource:
    url: jdbc:mysql://localhost/userdb
    username: admin
    password: admin


  jpa:
    hibernate:
      ddl-auto: update

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html